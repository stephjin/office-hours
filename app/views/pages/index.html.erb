<% if current_user %>

  <div class="container-fluid">
    <div class="row">
      <div class="row">
        <div class="col-xs-6">
          <h3>Locations</h3>

<!--           <div class="row">
            <div class="col-xs-4">hi</div>
            <div class="col-xs-4">bye</div>
            <div class="col-xs-4">still here</div>
          </div>
 -->

          <p>
          <% @locations.each do |location| %>
            <div class="box">
              <ul class="row" id="thumbgrid">
                <li class="col-xs-10">
                  <%= location.name %><br>
                  <%=link_to "See more", location_path(location) %>
                </li>
              </ul>
<!--                 <li><%#= location.address %></li>
              <li><%#= location.description %></li> -->
            </div>
          <% end %>
          </p>
          
        </div>
        <div class="col-xs-4">
          <%= render 'locations/form' %>
          
        </div>
      </div>
    </div>
  </div>


  <% else %>

  <div id="splash">
    <p id="splashtext">This is the home page for Project 2.1</p>
    <p id="splashtext">Filler text about the purpose of this website. User is unable to see anything unless they are logged in.</p>

    <%= render 'login' %>

    <%= render 'signup' %>
  </div>

<% end %>


